import { CourseTypePipe } from './course-type.pipe';

describe('CourseTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseTypePipe();
    expect(pipe).toBeTruthy();
  });
});
