import { TeachTypePipe } from './teach-type.pipe';

describe('TeachTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TeachTypePipe();
    expect(pipe).toBeTruthy();
  });
});
